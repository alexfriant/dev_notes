var alertRandom = function () {
    var randomNumber = Math.floor(Math.random() * 6 ) + 1;
    alert(randomNumber);
}; /* Note the semicolon is needed since this is a variable declaration */
alertRandom();